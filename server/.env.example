DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cod_crm?schema=public"
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Required for AI assist (never commit real keys)
OPENAI_API_KEY=

# One key is enough for dev; optionally split Address Validation key
GOOGLE_MAPS_API_KEY=
GOOGLE_ADDRESS_VALIDATION_KEY=
