services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cod_crm
    ports:
      - "5432:5432"
    volumes:
      - cod_crm_pg:/var/lib/postgresql/data

volumes:
  cod_crm_pg:
